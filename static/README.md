# Static Assets

This public repository does not include private or license-sensitive media assets.

To enable full playback, add allowed assets under these paths:

- `static/audio/`
- `static/album/`
- `static/images/`
- `static/icons/`

The file paths should match the values in `music.json` and `app.py`.
